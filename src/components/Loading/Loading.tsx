import { LoadingContainer, Spinner } from './Loading.style'

const Loading = () => {
    return (
        <LoadingContainer>
            <Spinner />
        </LoadingContainer>
    )
}

export default Loading